<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class GenreController extends Controller
{
    // if ($genre->books()->exists()) {
    // return back()->with('error', '書籍が紐付いているため削除できません。');
}
}
