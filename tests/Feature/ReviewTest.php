<?php

namespace Tests\Feature;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class ReviewTest extends TestCase
{
    use RefreshDatabase;

    // $review->likeUsers()->attach($user);

    // $this->assertDatabaseHas('review_likes', [
    // 'user_id' => $user->id,
    // 'review_id' => $review->id,
    // ]);
}
